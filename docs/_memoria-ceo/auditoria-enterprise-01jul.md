---
name: auditoria-enterprise-01jul
description: Auditoria enterprise 01/jul — REPROVADO 3.5/10; middleware é MORTO; críticos da janela do dono
metadata: 
  node_type: memory
  type: project
  originSessionId: 35674bb1-5490-4d72-a299-d5103c9a38bd
---

**Auditoria enterprise adversarial (01/jul, 17 agentes)** pedida pelo dono. Veredito: **REPROVADO para produção multi-tenant, nota 3.5/10.** Doc completo (50 melhorias + roadmap + pentest) em **docs/AUDITORIA-ENTERPRISE.md**; triagem em **docs/REMEDIACAO-AUDITORIA.md**.

## ⚠️ Achado-raiz CONFIRMADO por mim (build manifest = verdade)
**O middleware de auth é CÓDIGO MORTO.** O arquivo é `proxy.ts` com `export async function proxy(...)`; o Next só roda `middleware.ts` com função `middleware` — e não existe. `.next/server/middleware-manifest.json` → `"middleware": {}` VAZIO; ninguém importa proxy.ts. **A minha auditoria H-SEC-1 anterior deu GO errado** (alucinou que o proxy rodava). LIÇÃO: verificar crítico contra a verdade-fonte (o manifesto do build), não confiar em auditoria anterior. Consequência: cada rota `/api/**` depende SÓ do guard in-handler; as sem guard estavam ABERTAS com service-role (RLS off).

## ✅ Corrigido e NO AR — remediação de segurança de rotas COMPLETA (deploys #40-#45)
- Batch 1 (#40): guards + tenant em ~13 rotas de `hub/agentes/**` e `hub/cargos/**` (uazapi apagava WhatsApp sem login; sugerir-conhecimento = DoS de LLM).
- Batch 2 (#41): IDOR imóveis, PATCH /api/leads (whitelist+posse), rag-documentos, injeção PostgREST via `busca` (`lib/crm/sanitizar-busca-postgrest.ts` em 6 rotas).
- UX (#42): zoom reabilitado (WCAG 1.4.4), fim do loading infinito no Atendimento.
- Batch 3 (#43, auditada GO): varredura das 152 rotas → 7 críticas fechadas (rastreio PII enumerável cross-tenant, autonomia-matriz [limite financeiro da IA], alertas, ciclos-log, propostas/[propostaId], cotações, encaminhamentos) + **fail-closed em 19 arquivos** (`||ANON_KEY` → 503/throw).
- Batch 4 (#44): /api/leads GET (cross-tenant), ml/ciclo GET, tenant-scope no atendimento (resolveOperador+tenantId), rate-limit consultar-cnpj.
- Gate de créditos (#45): `assertSaldoAntesDoLLM` no chokepoint `completarChatPreferindoMistral` — modo SOMBRA (IA_HARD_CAP off), fail-open. Pronto pra quando a IA ligar. (Falta fiar em: completarChatComFerramentasMistral, playbook/gerar-fluxo, calibracao-chat, agente-briefing.)
- **Batch 5 + GATE de regressão (#48-#50, 01/jul tarde):** fechadas as 4 últimas rotas service-role SEM guard nenhum (`/api/agentes` GET→sessao/POST+PATCH→gestor, `/agentes/mobile`→sessao, `/agentes/[slug]/detalhes`→sessao, `/ml/aprovar`→gestor). `/api/agentes` GET vazava `uazapi_instance_token` (credencial WhatsApp) → aplicado `sanitizarAgenteHubParaCliente` (espelha /hub/agentes). **`lib/crm/guard-coverage.test.ts`** = gate que QUEBRA O BUILD se rota nova usar client service-role (detecta crmDb/**crmHandoffDb**/crmSupabaseAdmin/createClient SERVICE_ROLE) sem guard reconhecido nem estar na PUBLIC_ALLOWLIST justificada. **`.github/workflows/ci.yml`** roda tsc+vitest em push/PR. Erro cru redigido nas rotas PÚBLICAS via `lib/http/erro-publico.ts`. Verificado por auditoria adversarial (4 lentes read-only): ml/aprovar "cross-tenant" = **FALSO POSITIVO** (ML é Hub-global, `hub_ml_sugestoes` sem tenant_id).
- **hub_eventos — funil instrumentado (#50):** `negocio_criado` (POST negocios) + `negocio_ganho`/`negocio_perdido`/`negocio_etapa_mudou` (PATCH negocios/[id]) via `registrarEvento` best-effort. Com `lead_criado` já existente, o funil capta→fecha alimenta o keystone de KPIs. Relaciona [[central-performance-metricas]].

## 🔒 SESSÃO era FORJÁVEL — CORRIGIDO e no ar (deploy a54447c, 01/jul noite)
Achado durante a janela do dono: como o middleware está morto, as rotas confiavam no `sub` do cookie de sessão **decodificado local (base64), SEM validar assinatura** → cookie FORJADO com sub arbitrário se passava por qualquer usuário (inclusive owner). **Bypass de auth remoto.** Fix: identidade agora vem de `fetchAuthUserFromAccessToken` (Supabase `/auth/v1/user`) em `getCallerContext`+`resolveCallerAuthId` (async), `verifyServerOwner`, e 3 rotas atendimento (await). 2º vetor fechado: header `x-caller-auth-id` (valor do cliente) agora fail-closed (exige INTERNAL_API_KEY setada+correta). **Verificado AO VIVO por mim:** `/auth/v1/user` → 403 token forjado / 401 sem token; + 3 lentes adversariais SAFE + teste novo (caller-identity). **Dependência do dono:** remover `NEXT_PUBLIC_INTERNAL_API_KEY` do render.yaml (expõe a chave do gate ao browser).

## ✅ VERIFICADO AO VIVO (REST API, 01/jul) — muda a urgência do RLS
Com a service key do `.env.local` (read-only): **anon vê `*/0` (ZERO linhas)** em users, hub_leads_crm, hub_pessoas, hub_contas_receber, hub_ml_sugestoes, hub_negocios → **RLS enforced p/ anon**; o "anon lê tudo/self-promote" **NÃO é explorável hoje**. Totais reais: users=19, hub_pessoas=7, hub_leads_crm=6 (merge_pessoas é pequeno). Migrações RLS caem de prioridade (só faltam: isolamento cross-tenant do usuário LOGADO — não testável sem token — e versionar). Sem MCP/CLI Supabase nem psql nesta sessão; `gh` autenticado mas **403 no repo do demitido** (não dá pra auditar secrets → assumir service_role comprometida).

## 🧭 SUPABASE MCP conectado (01/jul noite) — RAIO-X AUTORITATIVO do #3
Advisors de segurança: **0 ERROR**, 87 WARN, 59 INFO. Migrações aplicadas vão até `20260701234027`; **AEC (e0–e7) NÃO aplicada** (tabelas `hub_obra_itens/medicoes/eap` não existem → camada latente). `hub_eventos`=17 linhas (meu funil grava ✅). **DESCOBERTA-CHAVE:** o app tem **DOIS caminhos de dados** — (1) `/api` service_role (blindado pelo fix de sessão) e (2) **browser DIRETO no Supabase**: realtime (`.channel().subscribe()` em leads/dashboard/atendimento/agentes/métricas) + **writes do office** (`components/office/DecisionPanel.tsx` faz update/insert direto em hub_leads_crm/hub_atividades/hub_fila_mensagens/hub_aprovacoes/hub_alertas). Por isso as **26 policies `USING(true)` p/ `authenticated` são LOAD-BEARING** — remover quebra realtime + office. Fix correto do #3 = **escopar por tenant** (`USING(tenant_id=current_user_tenant_id())`), NÃO remover, e **testar realtime+office**. **Urgência BAIXA: 1 tenant só** (`hub_tenants`=1) → cross-tenant é teórico até o 2º tenant. anon já bloqueado (0 policy anon-SELECT true; só 2 INSERT `public` em tabelas LEGACY `leads`/`ai_interactions`). Outros WARN: **search_path mutável ✅ APLICADO 01/jul via MCP apply_migration** (migração `20260701235500_harden_function_search_path`; 35→2, as 2 restantes = funções da extensão pgvector lang C, deixadas de propósito); SECURITY DEFINER exec público (resta), `hub_backups` SELECT true p/ authenticated (31k linhas — escopar no passe tenant-scope), leaked-password-protection OFF (toggle no painel, pendência do dono). Relaciona [[schema-rls-alinhamento-mestre]].

## 🔴 CRÍTICOS DA JANELA DO DONO (não faço sozinho — prod/irreversível/app-wide)
1. **Ligar o middleware** — REFINADO 01/jul tarde: `proxy.ts` (raiz) JÁ tem `export async function proxy()` + `export const config={matcher}` (convenção Next 16), MAS `.next/server/middleware-manifest.json` continua `"middleware":{}` VAZIO no build 16.2.4 → o proxy NÃO roda. O fix pode NÃO ser rename: talvez uma **flag experimental** no next.config p/ a convenção `proxy.ts`, OU rename p/ `middleware.ts`+`middleware`. Confirmar QUAL antes. Depois auditar allowlist `isPublicApiPath` fluxo a fluxo (pode bloquear intake público) + testar login/cadastro COM o dono. Enquanto morto, o **guard-coverage.test.ts** garante que toda rota tem guard próprio (a única barreira).
2. **Migrações RLS** (bloqueadas pelo classificador — [[migracoes-janela-do-dono-mesmo-com-autorizacao]]): matar `USING(true)` (users→auto-owner!, hub_pessoas/empresas, pipelines/vínculos), ligar RLS em hub_fornecedores, corrigir `CREATE POLICY IF NOT EXISTS` (SQL inválido → RLS financeiro nunca aplicou), backfill tenant_id + trocar tenantScopeOrFilter por `.eq` puro.
3. **Escrow:** remover `GREATEST(0, 0 - v)` (custódia fantasma, libera dinheiro que nunca entrou) + `FOR UPDATE` (double-spend).
4. **Infra:** ROTACIONAR service_role (JWT válido até 2036), tirar repo/.env.local do OneDrive, DELETAR `.github/workflows/backup-auto.yml` (faz git push de PII de leads), tirar NEXT_PUBLIC_INTERNAL_API_KEY do Render/render.yaml. Webhook/cron: HMAC real + CRON_SECRET (x-vercel-cron forjável no Render).

## Próximas passadas de código (eu, seguro)
Mais rotas service-role abertas (fallback `||ANON_KEY`, /api/cotacoes/* [token do fornecedor], /api/atividades, encaminhamentos), entitlements SaaS (não existem), gate atômico de créditos IA, prompt-injection/RAG cross-tenant, CI+observabilidade. Relaciona [[diretriz-melhor-para-o-sistema]], [[tenant-null-leak-pattern]].
