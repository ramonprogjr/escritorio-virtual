# 📦 docs/_arquivo — histórico arquivado (nada se perde)

Docs de fases passadas, movidos aqui durante o **destravamento** (05/jul/2026, Fase 0.4) para deixar `docs/` navegável. **Nada foi apagado** — só movido; o git history preserva tudo e a reversão é um `git mv` de volta.

> Fonte de verdade atual do projeto: **`docs/CONTROLE-MESTRE.md`**.

## `memoria-snapshot/`
Snapshot **estático** da memória do CEO de ~28/jun (56 arquivos), antes em `docs/_rumo-memoria/`. **Superado:** a memória viva do CEO vive em `~/.claude/.../memory/` (~60 fatos até 05/jul) e é a que o assistente realmente usa. Este snapshot já havia sido declarado "removido do repo" no commit `2f559cd`, mas seguia rastreado — ficou aqui só como registro histórico.
