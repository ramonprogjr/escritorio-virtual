---
name: sessao-handoff-28jun2026
description: "MANDATO F3→F4/F5→F6 CUMPRIDO (28/jun). Tudo deployado e verificado. Restam: aplicar 2 migrações (MCP caiu), chave Mistral (futuro), e 2 flags p/ o CEO. LER ao reabrir."
metadata: 
  node_type: memory
  type: project
  originSessionId: 35674bb1-5490-4d72-a299-d5103c9a38bd
---

Maratona autônoma 27-28/jun. Mandato do dono: "F3→F4/F5→F6, tudo, sem pendências, só a chave do Mistral para depois." **CUMPRIDO** — todas as fases entregues, build-gated, deployadas (`feature/escritorio-visual` → Render) e verificadas clicando. HEAD `601f4ac`, árvore limpa. Conecta [[pendencias-etapa-copiloto-agentes]], [[design-overhaul-deferido]], [[metodo-auditoria-adversarial-validacao]].

## ✅ ENTREGUE E DEPLOYADO
- **Agentes:** #16 IA-first na porta, #13 conhecimento editável, setor_ia, dogfooding (jargão→linguagem de dono, WhatsApp claro, catálogo colapsa, pt-BR), #6 follow-up, catálogo de cargos consolidado (Pré-vendas SDR market-agnostic).
- **F3 (IA-first onde o motor já existe):** fila de Distribuição (sugere+confirma 1 toque), Fornecedores no motor (chips mercados+recebe_leads+status), UUID→seletor (EntitySelect em Pedidos/Projetos/Ciclos), Negócios "próxima ação" Click-and-Go, Dashboard "O que precisa de você".
- **F4-A:** harmonização de cor — ~140 trocas/49 arquivos azul/roxo→verde+dourado + normalização das cores de etapa do funil em código (o banco guardava azul em hub_pipeline_estagios; o GET /api/crm/pipelines força a cor de marca pras etapas de sistema, sem reescrever o DB).
- **F4-B:** tabela→cards — Imóveis (grid+foto+status inline; fix: removido `metadata` inexistente do SELECT), Cadastros (colunas default enxutas), Canais (cards+banner de canal caído).
- **F5:** esconder plataforma/secrets do fornecedor comum por perfil (`hooks/useCrmRole.ts`, isOwner) — Configurações (13 env vars só p/ owner + fix loading), Ferramentas IA (slugs/custom só owner).
- **F6 (slice seguro):** "Gerar conta a receber" pré-preenchido do negócio ganho + "quem deve" no card. **FIX DE SEGURANÇA: export CSV (/api/crm/relatorios/export) usava service-role e NÃO filtrava tenant → vazamento cross-tenant; corrigido (.eq tenant_id em todas as queries).**

## ⏳ APLICAR QUANDO O MCP SUPABASE RECONECTAR (caiu na troca de conta; código tolerante, nada quebra sem elas)
- `supabase/migrations/20260630120000_hub_contas_receber_negocio_id.sql` — ADD COLUMN negocio_id (ativa o vínculo recebível↔negócio do F6).
- (A migração `proxima_acao_em` do F3 JÁ foi aplicada antes do MCP cair.)

## 🏃 MARATONA (28/jun, dono fora ~10h — "faça mesas redondas e auditorias e avance")
Ciclos de auditoria adversarial → corrigir → deployar. Log:
- **Ciclo 1 (8a1dfc8):** Auditoria #1 (motor+consolidação+segurança) + Batch 2. Achados: **Bug #1 [MÉDIO, EU introduzi] CORRIGIDO** — PATCH /api/parceiros/[id] escopava tenant pelo header forjável `x-tenant-id` → agora `g.ctx.tenantId`. **Batch 2 deployado:** visão guiada simples do fluxo (FlowGuiadoView + toggle Simples/Avançado; padrão no mobile). 6 falsos-alarmes derrubados com evidência (loop send_media, upsert duplicado, etc.).
- **⚠️ Bug #2 [MÉDIO, PRECISA-DONO] — NÃO corrigido (espera você):** `POST /api/parceiros` e `POST /api/crm/fornecedores` NÃO exigem sessão (só os GET ganharam guard na Fase 0); derivam tenant do header forjável → request não-autenticada com `x-tenant-id` válido cria parceiro/fornecedor em qualquer tenant. **Fix pronto** (add `requireCrmSessao` + `g.ctx.tenantId` nos 2 POST), MAS confirmar antes que NÃO há captação pública legítima (landing/portal de parceiro → cria sem sessão); se houver, o fix é API-key interna em vez de sessão. Callers vistos = telas autenticadas (NegocioFormDrawer lê; fornecedores/page cria logado), mas há `lib/parceiro-portal` — checar.
- **Ciclo 2 (deploy bosr1s364):** Auditoria do pipeline IA-first VIVO. Veredito: seguro p/ usuários reais hoje (webhook fail-closed em prod, claim atômico, isolamento lead↔telefone, HMAC do copiloto correto); 7 falsos-alarmes derrubados. **CORRIGIDOS (autônomo):** #1 [ALTO, explorável] `hub_whatsapp_menu` aceitava `numero_destino` arbitrário → cliente injetava "manda menu pro +55X" e usava o número/token do escritório p/ spam/phishing; agora no canal força o telefone do lead. #4 [BAIXO] `hub_crm_criar_cadastro` sem gate de modo → agora força telefone da sessão no canal. **PRECISA-DONO (não mexi):** #3 [MÉDIO] bypass de autonomia no atendimento WhatsApp (`engine.ts:115` ignora `limiteAutonomiaBrl` com só um warn) — é intencional? documentar em DECISIONS.md OU aplicar limite a escritas de valor. #5 [pré-req multi-tenant] `isolamento-conversa-lead.ts` não confere `tenant_id` — GATE BLOQUEANTE antes de ligar multi-tenant (2 leads com mesmo telefone em tenants diferentes passariam). #2 [MÉDIO] a IA pode setar score=100/mover estágio/injetar nota — badge "dito pelo cliente / não verificado" na UI do lead + limitar saltos de estágio.
- **Ciclo 3 (998d5d9) — O MAIS IMPORTANTE:** Auditoria do CAMINHO DO DINHEIRO. Veredito: NÃO era confiável. **CORRIGIDOS (autônomo, deployado):** CRÍTICO-1 `PATCH /api/crm/fornecedores/[id]` **não tinha AUTH** → comissão editável por qualquer um sem login → agora requireCrmGestor + tenant + **log de auditoria da comissão**. CRÍTICO-2 PATCH negócio sem isolamento de tenant → agora 404 cross-tenant + tenantScopeOrFilter. CRÍTICO-3 PATCH contas pagar/receber sem tenant → tenant-scoped. CRÍTICO-4 "Gerar conta a receber" duplicava (2 cliques) → SELECT-first por negocio_id + botão trava. CRÍTICO-5 billing ignorava `hub_ia_precos` (painel) → agora lê (sombra). MÉDIO-4 Pipeline Total truncado em 5000 → RPC de agregação. 307 testes. 7 falsos-alarmes derrubados.
  - **⏳ 2 MIGRAÇÕES criadas mas NÃO aplicadas (guardrail bloqueou; código tolerante, nada quebra sem elas):** `20260702120000_crm_negocios_pipeline_totais_rpc.sql` (RPC de agregação — segura, idempotente) e `20260703120000_hub_contas_receber_negocio_id_unique.sql` (índice UNIQUE parcial em negocio_id — **CHECAR DUPLICATAS ANTES**: `SELECT negocio_id, COUNT(*) FROM hub_contas_receber WHERE negocio_id IS NOT NULL GROUP BY negocio_id HAVING COUNT(*)>1`). Aplicar via Supabase quando puder.
  - **PRECISA-DONO (financeiro):** ALTO-2 quando ligar o bloqueio de saldo negativo de Tijolos (fora do copiloto, atendimento/builder não checam saldo); MÉDIO-3 markup `10` = 10× (1000%), não 10% — confirmar a semântica e rotular o campo no painel (risco de cobrar 100× a menos); comissão imutável no fechamento (regra de negócio — snapshot); unificar os 2 sistemas de custo divergentes (engine.calcularCusto câmbio R$5,75 fixo vs metering-calc configurável).

- **Ciclo 4/5 (b0f47qyvo):** Auditoria de COERÊNCIA/USABILIDADE da jornada IA-first (agentes/fluxo). **CORRIGIDOS (8 autônomos, no seu norte):** removida a barra fake "em breve" de IA/voz do editor guiado (a IA já está viva = fachada); o prompt `gerar-fluxo-ia` agora gera **send_document/send_audio** (antes a IA só fazia 4 dos 6 tipos → PDF/áudio só à mão) + os 4 handoff_to; publicação IA não-silenciosa (erro inline + retry) + "Concluir" travado durante geração; copy "editor abaixo" honesta; preview de áudio/PDF nos cartões; estado-vazio extrai só a narrativa (fluxo melhor). **PRECISA-DONO:** auto-gerar fluxo nos modos Cargo/Só-playbook (agente nasce "mudo" sem avisar — decisão de produto); inverter ordem do modo "Descrever para IA" (cria agente ANTES de descrever); afrouxar o gate do "Só playbook" (trava sem análise Mistral).

- **Ciclo 6/7 (24155d8):** Auditoria FUNCIONAL-NÃO-FACHADA das 7 telas de trabalho. Veredito: funcionais, nenhuma fachada. 9 polimentos corrigidos (que quebrariam numa demo): botões "Ver →" mortos (Leads/Negócios) → navegam; etapa mostrava slug cru ("fechado_ganho") → label legível; Arquivar negócio com confirm() nativo que mentia sucesso → CrmConfirmDialog+toast; excluir regra/toggle na Distribuição sem feedback → dialog+toast; export CSV (receber/pagar/dashboard) engolia erro → toast.error; botão "Negócio" na Caixa com loading global → por-id. **Nota:** link Negócio→Obras carrega `?negocio_id` + banner, mas o FILTRO real da lista de obras é follow-up (toca /api/crm/obras); 1 confirm() nativo restante em canais-entrada (próxima passada).

## 🏁 RESUMO DA MARATONA (28/jun, dono fora ~10h) — PICO DE CONSOLIDAÇÃO
**6 ciclos** de auditoria adversarial → corrigir → deploy, TUDO no ar e verificado (build-gated). ~21 commits. Cobertas as superfícies CRÍTICAS: motor/consolidação, pipeline IA-first vivo, caminho do dinheiro, coerência da jornada IA, e funcional-não-fachada das telas. Também: editor de fluxo reativado + Batch 2 (visão guiada) + Batch 3 (PDF/áudio/split), IA acesa, bucket playbook-media. **Bugs REAIS corrigidos autônomo (~24):** tenant-forjável, spam/phishing por injeção, cadastro-gate, 5 críticos financeiros (comissão-SEM-AUTH, 3 cross-tenant, recebível duplicado, billing-preço-morto), pipeline-truncado, 8 de coerência IA (fim da fachada + IA gera PDF/áudio), 9 de funcional-não-fachada. **O método pagou: bugs que nenhuma construção viu, ~25 falsos-alarmes derrubados com evidência.** Parei num PICO (não por falta de alvo) — o resto de alto valor precisa de você (validar IA, decisões PRECISA-DONO, 2 migrações). Tudo por ciclo acima.

## ⭐ AUDITORIA PRIORIDADE #1 — CÓDIGO ÚNICO / DEDUP (28/jun, dono fora 2ª vez)
Veredito: **NÃO confiável ainda.** A dedup manual funciona, mas há furos estruturais. **CORRIGIDO autônomo (subagente, a deployar):** normalização de telefone consistente no auto-cadastro WhatsApp (duplicata escapava: cadastro à mão com máscara vs WhatsApp só-dígitos = 2 pessoas); webhook insert agora checa erro + re-busca em 23505; super-cadastro mapeia 23505→409 "já cadastrado"; dedup do app agora filtra `tenant_id` (não vaza nome/código de outro escritório no 409); RPC `crm_proximo_codigo` versionada em migration (estava só em docs/sql → ambientes novos caíam no fallback COUNT+1 com corrida); migration de UNIQUE em `hub_pessoas.codigo` (criada, NÃO aplicada). **⚠️ PRECISA-DONO (estrutural, sua PRIORIDADE #1):** (1) **MERGE de duplicatas NÃO EXISTE** — precisa de tela+endpoint pra consolidar 2 cadastros preservando negócios/leads/notas/vínculos N:N (feature nova, decidir qual registro vence); (2) **constraint UNIQUE de documento/telefone/cnpj é GLOBAL** → migrar para `(tenant_id, documento)` etc. (risco em produção: precisa janela + checar/backfill duplicatas antes — o mesmo CPF em 2 tenants hoje colide/vaza); (3) contador de código sem tenant na PK (numeração global, não por escritório); (4) coletar CPF na esteira conversacional da IA (decisão de fluxo). Falsos-alarmes derrubados: prefixos PES/PS divergentes (cosmético, não colide), RLS protege dedup (irrelevante, service-role).

### Plano de consolidação (mesa redonda + dono autorizou) — P0 LIMPO
**P0 (detecção de colisões, read-only via API):** ZERO duplicata intra-tenant (pessoa_doc/tel, empresa_cnpj=0) e ZERO cross-tenant → terreno limpo, consolidação SEGURA. **Decisões do CEO (dono confia):** merge → vence quem tem MAIS dados (desempate=mais antigo, humano confirma, perdedor ARQUIVADO nunca deletado, doc diferente bloqueia, valor do perdedor → dados_extras.merge_origem). **EM CONSTRUÇÃO autônomo (atrás de flag `NEXT_PUBLIC_CRM_MERGE_DUPLICATAS`):** RPC `crm_merge_pessoas` atômica + GET /api/crm/pessoas/duplicatas + POST /merge (gestor/owner) + tela `/crm/cadastros/pessoas/duplicatas` (3 passos) — detecção+UI ao vivo, botão Mesclar gated até o dono aprovar o 1º par. **⚠️ PRECISA-DONO / JANELA (irreversível — NÃO fiz):** (a) criar índices UNIQUE por-tenant `CONCURRENTLY` (via execute_sql, não apply_migration) — seguro agora (P0 limpo); (b) DROP dos índices UNIQUE GLOBAIS de documento/telefone/cnpj — **é o que destrava a VISÃO** (mesmo CPF em 2 escritórios), mas irreversível na prática; (c) trocar PK do `hub_codigo_contador` p/ `(tenant_id, entidade, ano)` + RPC `crm_proximo_codigo` com `p_tenant_id`. Ordem: (a)→validar→(b)→(c). Arquivos de migração a escrever; aplicar em janela de baixo tráfego com seu OK. Plano completo no output do workflow w31dbslgu.

### 🚀 DEPLOY 28/jun noite (commit 61b09bd) — verificado por auditoria adversarial pré-deploy
NO AR: dedup hardening; **FIX de VAZAMENTO CROSS-TENANT AO VIVO** na criação de lead (`vincularPessoaPorTelefone` não filtrava tenant nem gravava tenant_id — NÃO estava atrás de flag; pego e corrigido ANTES de ir pro ar); webhook db() sem fallback anon (evitava perda silenciosa de lead) + tenant_id; feature de MERGE de duplicatas (atrás de flag OFF, inerte); modo texto do copiloto completo; erro Mistral categorizado; dev doc. Gate: tsc + vitest 336/336 + build verde. A verificação adversarial (w2p6k7xjy) achou 3 bloqueadores reais — todos corrigidos no commit. ⚠️ **PRECISA-DONO antes de LIGAR a flag de merge:** (1) validar a RPC `crm_merge_pessoas` em STAGING (merge normal / documentos diferentes / cross-tenant bloqueia / colisão de vínculos) — é a operação mais destrutiva do sistema; (2) aplicar a migração `20260705120000_merge_pessoas.sql` (manual); (3) ligar `NEXT_PUBLIC_CRM_MERGE_DUPLICATAS` no Render (build E runtime); (4) confirmar `INTERNAL_API_KEY` em produção (sustenta a defesa do POST /merge — pré-existente em todo o CRM, não introduzido aqui); (5) backfill de `tenant_id` nas pessoas legadas (NULL) antes de operar com >1 tenant. Menores anotados: decidirVencedor sem contagens; 409 ainda mostra nome intra-tenant; detectarPares O(n²).

### 🚀 DEPLOY 28/jun (commit 0999a58) — leftovers funcional-não-fachada
NO AR: filtro REAL de Obras por `?negocio_id` (era fachada — banner aparecia mas a lista mostrava TODAS as obras do tenant; GET /api/crm/obras agora aceita+aplica negocio_id, tenant-scoped; POST envia negocio_id); `confirm()` nativo de Canais de entrada → CrmConfirmDialog + toast (reusa padrão de agentes). Gate: tsc + vitest 336/336 + build verde.

### 🚀 DEPLOY 28/jun (commit e9ebaa3) — auditoria da superfície restante: VAZAMENTO CROSS-TENANT CRÍTICO fechado
A auditoria das telas não cobertas (Relatórios/Pipelines/Tráfego/Usuários/Especialistas/Agentes/Dashboards) achou e CORRIGIU vulnerabilidades REAIS: várias rotas (`pipelines`, `pipelines/[id]/estagios`, `dashboard`, `metricas`, `hub/agentes` lista e `[slug]`) rodavam em **service-role SEM guard de auth e com tenant vindo do header `x-tenant-id` (forjável)** → qualquer sessão lia/editava/**DELETAVA** (cascata) dados de outro tenant. Fechado: `requireCrmSessao`/`requireCrmGestor` + tenant da sessão + `agenteForaDoTenant`/`assertPipelineDoTenant` (404). Mais: `dashboard-aggregate` com tenant em 4 tabelas; Relatórios com **total REAL** (count exact + aviso "X de Y, limitado a 500") e counts laterais movidos do client-anon p/ endpoint server guardado; especialistas dedup por tenant (fim do vazamento de nome no 409); UX (toast no toggle de agente / salvar especialista). Verificado: 4 lentes adversariais + síntese + trace manual da auth (chamadores client mandam cookie → guard passa, sem regressão) + gate tsc/vitest 336/vitest/build verde. **NÃO filtrado** `hub_alertas`/`hub_ciclos_ia` (sem coluna `tenant_id` no schema → filtrar quebraria a query; vira migração).
**⚠️ NOVOS PRECISA-DONO desta auditoria:** (1) **Tráfego/Windsor** é conta global única (1 `WINDSOR_API_KEY` p/ a rede) → decidir: esconder de tenants não-dono OU modelar credencial por-tenant (feature); apliquei gate por gestor como paliativo. (2) **Tela de config de Pipelines não existe** (`app/crm/pipelines/*` = 0 arquivos; etapas/cores são read-only) → feature nova se for requisito (as APIs, agora seguras, já sustentam). (3) **Migração aditiva**: `tenant_id` em `hub_alertas` e `hub_ciclos_ia` p/ fechar o isolamento desses 2 KPIs do dashboard (hoje single-tenant, sem leak real até multi-tenant). (4) confirmar se dedup de especialista deve ser global ou por-tenant.

### 🏁 ETAPA AUTÔNOMA ESGOTADA (28/jun noite)
A auditoria declarou: não há mais correção de segurança/funcional/UX possível **sem decisão de produto, migração em produção, ou a chave Mistral**. **Tudo o que não depende do dono foi entregue, verificado e deployado** (deploys desta maratona: 61b09bd, 0999a58, e9ebaa3 + as migrações aplicadas). RESTA tudo owner-dependente: Mistral (dev passa a chave), token (rotação adiada c/ lembrete), validar RPC merge em staging + ligar flag, janela p/ constraint global→tenant + contador + backfill tenant_id legado + migração alertas/ciclos, e as decisões de produto (Windsor, tela de pipelines). **Próximo passo: voltar ao MACRO com o dono.**

## 🗺️ SESSÃO MACRO (28/jun noite) — núcleo + direcionamento
Dono definiu o foco: **núcleo rodar PERFEITO** antes de vendas+marketing (ver [[macro-sequencia-nucleo-primeiro]], [[fluxo-core-captacao-direcionamento]]). Entregue+deployado+verificado:
- **DEPLOY 6fb3150** — núcleo endurecido (19 fixes da revisão dos 5 fluxos: 7 cross-tenant, corrupção status 'perdido'→'fechado_perdido', códigos atômicos OBR/ESP/FOR, robustez IA/WhatsApp, captação Hub-only com tenant forçado + rate-limit `lib/rate-limit-memoria.ts`).
- **BACKUP** do estado bom: branch+tag **`backup-sistema-ok-28jun`** (GitHub). Rollback = `git reset --hard backup-sistema-ok-28jun`.
- **DEPLOY 0204edf** — REDESENHO do direcionamento (Click-and-Go: 1 verbo 'Direcionar', recomendado pré-selecionado+1 toque, confiança legível+chips, preview, voz, fila em destaque, 'Qualificar e direcionar', linguagem honesta). Reusa motor/score/endpoints (ZERO backend). Verificado por mesa redonda 'não-piorar' (achou 2 perdas de borda) → RECUPERADAS antes de subir (botão 'Liberar' pendência; 'Direcionar a outro destino' Click-and-Go por perfil via /encaminhamentos quando o motor não acha) + verificação adversarial 'PODE SUBIR' + gate verde. Componentes: DirecionarLeadCard/Drawer, FilaDistribuicao, DistribuirLeadPanel, direcionamento-ui.ts, encaminhamento-manual.ts.
- **Comunidade→CRM:** elo NÃO existe; dono PAROU ("não precisa por enquanto, eu explico depois") — ver [[comunidade-elo-crm-pendente]].
- **Diretriz nova:** mesa redonda + chamar o dono nas decisões de linha de negócio ([[feedback-mesa-redonda-e-checkpoint-negocio]]).
- **Ressalvas cosméticas (não bloqueiam):** spinner 'Liberando' só no top-1 da fila; direcionar manual a perfil sem lista grava sem empresa vinculada. **Sem teste no navegador** (verificação foi por código) — vale click-test nos 3 pontos (Fila/ficha/lista).
- **Sequência do macro a seguir (dono):** marketing (IA tráfego Google/Meta) → multi-tenant → gestão de usuários → Arq&Eng → demais.

## ✅ RESOLVIDO (mesa redonda a+b, 28/jun)
**Parceiro×fornecedor + controle do motor — RESOLVIDO e deployado.** Mesa redonda (workflow 5 lentes+CEO) inverteu para b-first e achou 2 vazamentos cross-tenant. Entregue: **Fase 0** (motor sempre filtra tenant via defaultTenantId; GET parceiros/fornecedores agora exigem sessão) · **Fase B** (PATCH /api/parceiros/[id] gestor-only whitelist + editor inline mercado-single-select/recebe_leads/status + badge "No motor" + chip; contrato puro parceiro-fornecedor-map) · **Fase A 8-10** (colunas-espelho + backfill dos 7 em hub_fornecedores com origem_parceiro_id=rollback; motor lê fornecedores atrás de `MOTOR_FONTE` default='parceiros' ZERO-regressão + fallback; sync parceiro→fornecedor no PATCH; validado lado-a-lado: 5 candidatos score IDÊNTICO nas 2 fontes). **DEFERIDO (passo 11, decisão deliberada do dono):** flip `MOTOR_FONTE=fornecedores` (1 linha de env, reversível) + remover fallback + unificar os 2 editores (Parceiros×Fornecedores). Migração versionada: `supabase/migrations/20260701120000_hub_fornecedores_espelho_motor.sql`. ATENÇÃO p/ o flip: `total_leads_recebidos` (carga) só sincroniza na edição do parceiro, não a cada lead distribuído — pode divergir sob a flag; conferir antes de remover o fallback.

## ⚠️ FLAGS PRO CEO (revisar; não bloqueiam)
2. **Cores de etapa no DB ainda azuis** (corrigidas em CÓDIGO no GET; o guardrail bloqueou o UPDATE em massa). Se quiser, atualizar o seed `hub_pipeline_estagios` com OK explícito.
3. **KPI dedup (F4-C):** parcialmente coberto pela despoluição do Dashboard (F3); não havia bloco "métricas da rede" separável óbvio p/ condicionar mais.
4. Tons de dourado das etapas do funil ficaram próximos — afinar nuances se quiser mais contraste.
5. ~~BUG Kanban de Leads (KPI mente)~~ **RESOLVIDO e deployado (bd027c9):** os 6 leads estavam em `etapa='encaminhado'`; o KPI semResposta os contava mas o board não os mostra. Fix: `foraDaFilaAtiva` (ganho/perdido/spam/convertido/ENCAMINHADO) aplicado a semResposta/emRisco/pipeline + novo KPI "Encaminhados" mostra onde estão. Verificado: Sem Resposta 0 + Encaminhados 6. O painel "O que precisa de você" (F3) JÁ estava correto (/alertas/parados exclui terminais).

## ⏸️ FUTURO (espera o dono)
- **🔑 MISTRAL_API_KEY + COPILOTO_HMAC_SECRET no Render** → destrava IA ao vivo: Atendimento "Sugerir resposta" (único item de F3 que ficou pra trás por depender da chave), Agent Builder/#16 geração, copiloto, RAG/playbook dos agentes.
- Deferido por dados/infra: módulo Obra/Engenharia/Arquitetura, notificações WhatsApp/email/push, Tijolos Fase 3 (gateway de pagamento), lançamentos financeiros automáticos por evento.

## Lição aplicada nesta maratona
Todo push agora é **gateado no build** (`npm run build` exit 0 antes do git push) — depois de um commit com tsc:2 ter ido ao push (Render não publica build quebrado, então produção não caiu; corrigido em 38b90cd). E SEMPRE rodar tsc COMBINADO após subagentes paralelos + verificar no navegador antes de deployar (pegou o erro real `hub_imoveis.metadata` e o azul do Kanban vindo do banco).
